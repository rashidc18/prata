# Prata

Prata is a small compiler written in ANSI C. It is not intended to become a widely used programming language. Instead, it serves as a focused study project on compiler construction.

```
$ make
$ ./prata --help
```

